# age-gender-detection-opencv
Age and gender classification from facial images using OpenCV DNN and pre-trained deep learning models. The system detects faces, predicts age range and gender, and classifies users into age groups for customer analytics.
